import React from 'react';

import { MainContainer, LoadingSpinner } from './Styling';

export const Loading = () => {
  return (
    <MainContainer>
      <LoadingSpinner></LoadingSpinner>
    </MainContainer>
  )
};